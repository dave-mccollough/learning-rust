# Functions

- The main function is the entry point to the program
- Functions are defined using the `fn` keyword
- Use snake_case for function names

- If your function is returning a value use `->` and the return type

`fn do_stuff () -> bool {`

`}`

