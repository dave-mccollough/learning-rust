# Slices

- Region of array or vector than can be any length
- Cannot be stored as variables or passed into functions as arguments


- **Ordinary reference points to a single value**
- **A slice points to a range of consecutive values**
- **Both are non-owning**

